import React from 'react';

const CustomHtmlDocument = () => {
  return (
    <html>
      <body>You can override the default with a custom component.</body>
    </html>
  );
};

export default CustomHtmlDocument;
